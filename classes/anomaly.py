class Anomaly:
    def __init__(self, x, y, radius, effectiveRadius, strength):
        self.x = x
        self.y = y
        self.radius = radius
        self.effectiveRadius = effectiveRadius
        self.strength = strength
